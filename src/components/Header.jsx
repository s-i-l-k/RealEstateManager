import React from 'react';

const Header = ({children}) => {
    return (
        <div>
            <div>
                logo
            </div>
            <div>
                menu
            </div>
        </div>
    )
};

export default Header;
